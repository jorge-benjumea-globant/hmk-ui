// Default theme for Calendar Container
import * as theme from "../../themes";

const defaultCalendar = {
  default: {
    color: "black",
    "font-size": "5em"
  },
  navigation: {
    background: `linear-gradient(#73589c, ${theme.HMKPrimary})`
  }
};

export default defaultCalendar;
