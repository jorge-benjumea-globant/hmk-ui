// Default theme for Li dropdown
import * as theme from "../../themes";

const defaultSocialNetworkBar = {
  default: {
    color: "green",
    "font-size": theme.fontSize
  },
  item: {
    display: "inline"
  }
};

export default defaultSocialNetworkBar;
