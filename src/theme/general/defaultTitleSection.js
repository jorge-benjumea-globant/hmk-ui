import * as theme from "../../themes";

const defaultTitleSection = {
  default: {
    color: theme.fontColor,
    "font-family": "WisdomScriptAIRegular",
    "font-size": "1.7em",
    "font-weight": "normal",
    "letter-spacing": "0"
  }
};

export default defaultTitleSection;
